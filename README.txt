Adds support for Uyuni candlelight's.

Automate on/of state or set timers just as with the Uyuni remote.
Use the app without adding devices/drivers, using the flow-cards to automate the lights.
Or add a remote control device to control your lights manually.